export async function generateSuno(prompt) {
  const filename = `audio-${Date.now()}.mp3`;
  const filePath = `/tmp/${filename}`;
  // Ici, tu implémentes l’appel à Suno ou copie le fichier dummy
  return filePath; // à remplacer par le vrai fichier mp3 généré
}
